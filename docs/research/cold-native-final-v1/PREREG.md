# Astra High cold recovery with a complete model-written final

2026-09-10. One fresh matched interface pair in the established cold-recovery
family. The task/data grammar comes from the existing 40-event, 120-note fixture;
this is exposed development evidence, not an independent holdout or forty live
model turns. No old control may be reused as its denominator.

## Hypothesis and gates

The existing bound retrieval executor can complete this exact request before
inference. The remaining model obligation is the normal complete JSON answer,
with access to the original evidence and ordinary semantic review/tools. Unlike
the historical zero-call/caller-rendered result, both current arms invoke Astra
High and preserve the final native agent-message bytes unchanged. No selector,
forced terse answer, output repair, reduced effort or probe suppression.

Prior Luna controls paged/searched/verified the same request family over five
model segments for cold recovery. That motivates the mechanism; it does not
predict Astra counters. The narrow final has five required fields, making this
a more plausible output attack than the failed code-review packet. Both source
files, global settings, client base, skill, tools, task and checks are matched.
Control is Engine-managed ordinary execution under installed Helix continuity;
it is not pristine Codex. Candidate adds caller-completed recovery evidence.

Before any inference: run the existing resolved/bound/context-bound retrieval
tests, compare Engine output against the independently indexed note oracle, bind
task/source/state/version/config/programs, verify exact Unicode/whitespace/base64,
and perform zero-turn native preflight on both directories with High explicitly
reported. No credentials copied or production configuration changed.

The source is archived and reopened through the existing Store. The packet carries
actual results, exact original note, source identity and the explicit unresolved
authority/delivery limits. The original complete history remains available.
Passing mechanics does not assert semantic adequacy. Unknown/ambiguous requests,
inherited domain constraints, stale state and corrupt evidence cannot produce a
qualified prepared packet. No hidden oracle or previous model answer enters it.

## Run protocol

Randomize and freeze arm order before inference. Exactly one user turn per arm,
unrestricted internal tools/segments, all attempts charged, no restart/retry or
post-run answer repair. Stop on infrastructure, config/source binding or task
failure. A normal tool error remains in the evidence and does not itself become
a workflow failure if the model legitimately recovers within that same turn.

Both final answers must satisfy all requested fields, types, code points, trailing
whitespace, leading zeros, exact base64 and source event identity, without source
changes. Inspect original final items and visible execution for truthful claims.
Report input/output separately, cache/uncached/reasoning subsets, segments, commands,
preparation, archive/retrieval bytes, latency and dated cost scenarios. Scientific
80/80 and first-release65/65 are separate labels. No quota/bill or universal parity
claim follows from one pair. Failure of either capability arm invalidates economic
qualification; costs stay visible. No automatic further model pair is authorized.

This is a missing-family interface pilot, not permission to splice incompatible
versions into an existing seven-cell median. A complete integrated policy must
still be frozen and qualified across all seven cells and hostile workflow cases.

## Latest user steering: semantic checkpoints in W50

After this focused pair, analyze a separate harder W50 trajectory with several
genuine semantic checkpoints: ambiguity, changed requirements and decisions. Keep
normal model-written answers at those checkpoints and exact recoverable history.
Engine may handle only already-resolved transitions. Do not turn semantic events
into ACKs to improve savings. Count semantic cost explicitly: with unchanged
semantic output S and removable ACK output A, ACK elimination alone saves A/(A+S).
This arithmetic is conditional; it does not establish a no-loss semantic emulator.
The harder W50 version will need its own frozen protocol and offline gate before
new native execution; it cannot silently replace the current cohort task.
