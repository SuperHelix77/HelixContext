# One Sol High V2 pair, frozen before inference

Follow `DESIGN.md` and the V1 normal-final-answer contract. One fresh matched
maintenance-task pair, random order, both arms ordinary tools/default reasoning
effort High and model-written final reports. Control default base; candidate uses
the same Sol kernel/skill as V1. Change only the source presentation and bound diff
receipt. No caller-rendered final, additional agent, probe suppression or new plan.

The maintenance task, original source, public tests and94-case independent oracle
are unchanged known development evidence. Only the task source may change. Both
workspaces declare the empty publication lock in their initial Git tree. Memory
preflight remains caller-owned and charged. All source/config/native identity
bindings, checker outcomes and final-answer claims require independent audit.

Use the unchanged V1 stale/duplicate/recovery semantics through the V2 review
adapter. A failed exact selector stays failed; no automatic rewriting. Cold diff
fallback is explicit and exact. Ordinary inspection remains available regardless
of the receipt. Structured internal tools do not dictate final answer structure.

At most two native user turns per arm; a second only follows actual artifact-check
failure. No cap on internal semantic/tool segments and all reported tokens count.
Stop the pair on infrastructure, identity, scope or configuration failures. Retain
all failures; do not restart inference to fix an observer. No control reuse.

Falsify the intended removal of work from native traces: selector error/repair,
separate diff/scope inspection, commands/segments, novel semantic probes and final
completeness. Count the larger evidence packet and receipt. Neither a byte count
nor offline replay establishes model savings. Native comparisons remain separate
from V1; one successful task cannot satisfy a seven-cell median release gate.

Before launch: eleven V2 tests, eighteen V1 transport/effort/lifecycle tests, exact
V1 proposal replay, independent diff application and unchanged finite checks pass.
Freeze scripts, adapter, kernel, skill, prompt, workspace, client and effective
config together. No changes to the frozen V1 inputs. No production hook activation.
