# W50 native-final V2: literal ACK instruction

This inherits the full W50-native-final V1 protocol except one prospectively
declared caller-serialization amendment. V1 stopped after two control turns:
`ACK E01`, then `ACK E02.`. Its 40,413 input/15 output tokens and raw evidence are
retained. Candidate never ran; there is no V1 savings result.

V1's caller trailer, `Return exactly ACK E02.`, placed sentence punctuation next
to an unquoted exact target. V2 quotes the required decoded string and explicitly
excludes quotation marks and trailing punctuation. Only the trusted final caller
trailer is changed; event data/request bytes, canonical ACKs, strict validation,
final semantic request, original client base, effort, tools, current skill and
caller ledger are unchanged. The final model answer remains completely untouched.
This is a new versioned pair, not resumption or an unreported retry of V1.

The extra instruction tokens count in actual native usage. No V1 control or old
selector denominator is reused. A second exact-output failure stops this approach;
do not sample more prompt variants. High is the first fresh pair. XHigh remains a
separate cell under this same predeclared protocol, contingent on surviving High.
All W50, memory, scope, recovery, semantic and final-authorship gates in V1 apply.

Offline tests must verify the decoded ACK literal for all49 records, preserve
untrusted embedded text exactly, and leave the final semantic prompt unchanged.
