# Research/offline gate accepted; V2 eligible for one High pair

2026-09-10: the user answered “Allow experiments after research and offline gates”
to the explicit clarification concerning small preregistered benchmarks with
unchanged effort, tools and model-written finals. This supersedes the experiment
pause in HOLD.md; it does not certify a result or permit benchmark retries.

The semantic-emulation research does not justify eliminating new Astra judgment.
W50 uses a different, already demonstrated boundary: 49 explicitly specified
record-and-ACK transitions are durable caller operations, and Astra retains the
entire final semantic judgment and answer. Existing tests verify restart, duplicate,
conflict, stale/tampered state, interrupted storage and semantic escalation. The
new wording tests preserve all event records and the final prompt.

The exact ACK target is now quoted. V1's stopped control and cost remain separate.
This is the first V2 pair, not a hidden rerun. A further exact-output failure ends
this wording approach. Do not change the grader or normalize the delivered final.

Run one fresh Astra High pair after unchanged-source and native configuration
preflight. Preserve the original client base and current skill in both arms.
Additional tool segments remain available and count. Register XHigh separately
only if High survives the full gates; no effort mixing or missing-cell substitution.

The research target remains80/80. The65/65 first-release criterion applies to the
full seven-task Astra median, not this one task. Historical full-final output cost
makes80% output uncertain even when ACK elision succeeds. Report the measured
frontier rather than shrinking the final schema or extending the episode to
inflate the baseline. No guarantee of broad intelligence parity follows from W50.
