# Luna selector-only candidate: observed 98.28 / 88.73

One adaptive candidate following the failed assembled pair, with its fresh native
control reused explicitly. Same default client base, High effort, ordinary tools
and native skill attachment as the preceding candidate. Model selects policy and
amendments only; Engine derives authorization, reason and final field copying under
the bounded rule schema. No model retry occurred.

| Metric | Prior fresh control | Selector candidate | Saved |
|---|---:|---:|---:|
| Input | 1,074,689 | 18,466 | 98.28% |
| Uncached input | 46,081 | 8,482 | 81.59% |
| Output including reasoning | 639 | 72 | 88.73% |
| Reported reasoning | 172 | 52 | Separate counter |
| Candidate segments / commands | — | 1 / 0 | No extra setup segment |

Native hashes, final usage, exact answer and frozen task grader reconcile in the
[receipt](MINIMAL_SELECTION_LUNA_RESULT.json). Model chose E01 plus E17; the caller
retained exact numeric strings and nonce and rejected authorization for the bound
single-approver request. The independent fixture grader passed.

The preceding candidate emitted 228 tokens, including 192 reported reasoning. This
one emitted 72, including 52 reasoning. These observations support testing removal
of redundant semantic-interface fields. They do not prove causality from one adaptive
sample or expose hidden reasoning. Both prior failure and current win remain visible.

**Freeze as a bounded 80/80 development candidate, not a general release.**
N=1 median equals this observation, not a representative model median. Broader
coding/retrieval medians remain unfavorable and must not be replaced by this result.
Positive authorization, unfamiliar scopes, semantic re-entry and new task instances
still require native paired qualification. Codex desktop delivery remains separate.

The evaluator supports only explicit minimum/group rules with the known amendment
schema. Unknown policy fields or scopes raise for semantic handling. The model
still chooses authority; a mechanically valid wrong selection can produce a wrong
answer, as an adversarial unit test demonstrates. Hashes do not certify selection.

21 offline policy checks preceded the call. The preceding assembled lifecycle's
preparation/recovery and this candidate's archive-copy/setup costs remain additional
costs; the native-token table is not total monetary cost or included-plan quota.
Current candidate elapsed time was 4.11 seconds, a single local observation.

This experiment uses the separately authorized caller-owned ACK contract. It does
not satisfy the original fixture's historical requirement for 50 model turns in
both arms; eliminating those calls is the intervention, not protocol replication.
