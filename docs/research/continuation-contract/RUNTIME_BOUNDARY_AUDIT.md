# Routing and first-segment intervention boundary

OBSERVED: revalidated existing admission against current repository state.
Eight tests pass; the real frozen Sol qualification verifies120,179logical bytes.
All production requests and Astra/Luna attempts bypass with zero gate file reads.
This preserves the narrow existing research qualification; it is not80% savings.

Repository call-site inspection finds `admission.dispatch` used by its tests;
`admission_probe` calls `decide`. It is a standalone caller API, not global
interception of benchmark drivers or Codex tasks. Explicit research drivers run
unqualified candidates to measure them; their outputs must not be mistaken for
production admission. No routing code or installed hooks were changed.

## Tool-loading hypothesis

HYPOTHESIS: deferred tool definitions might reduce repeated runtime input while
preserving eventual tool availability. Current evidence does not qualify it.

The locally generated `ThreadStartParams` schema includes `deferLoading` on
`DynamicToolSpec` function tools and namespace functions. The latest actual
prepared-pair thread/start request supplies no `dynamicTools`. Therefore deferring
caller-added tool definitions cannot remove a definition payload that this runner
never added. This does not establish how the runtime injects built-in tools.

Official [App Server documentation](https://learn.chatgpt.com/docs/app-server)
confirms experimental dynamic tool registration and client-serviced tool calls.
The fetched page does not document `deferLoading`; installed schema evidence and
public documentation are distinct. Neither establishes a supported switch to
lazily replace every built-in tool in this runtime. No such switch was invented,
no ordinary tool was disabled, and no native token experiment was spent on it.

The approximately19k first-segment input includes runtime context. Its composition
is not observable from these token receipts alone. Calling all of it tool schema,
Helix bootstrap, removable redundancy or an irreducible lower bound is unjustified.

## Narrow remaining integration opportunity

INFERRED: current research RPC rejects any server-originated request, including
an unexpected dynamic tool call. Thus a real typed caller capability requires a
separate qualified handler before use. A small receipt-validation operation could
replace observed generated hashing code while leaving exact source inspection and
semantic tests available. It would add a tool schema and tool-result/model cycle;
these costs must be included. It does not by itself explain how to meet80/80.

Before implementing that handler, require a precise cost comparison against the
existing command's observable generation and context cost. Do not create another
large generic API or discovery workflow. Failure must surface the original error
without rerunning side effects; changed source requires new evidence. Ordinary
semantic tools remain available. This is an untested candidate, not a promotion.

Machine audit:`RUNTIME_BOUNDARY_AUDIT.json`. No new native model calls.
