# V3: effective configuration binding, unchanged coding profile

V2 stopped after one control completed because ~/.codex/config.toml changed.
80,153 input / 2,256 output tokens remain charged. Offline recovery retained raw
calls and passed the four public plus 611 finite interval checks with zero further
model calls. The captured thread still reports Luna High. Original config bytes
were not saved, so the exact changed keys and their cause remain UNKNOWN. That run
does not become a qualified control or a reused denominator.

V3 retains the V2 isolated tracked repositories, exact task prompts, fixed V7
kernel, attached skill, High effort, ordinary tools, caller checks and randomized
fresh pairs. It corrects the research guard: snapshot full config privately and
bind all parsed settings except two GUI defaults already explicitly overridden
by thread/start and every turn/start: model and model_reasoning_effort. Verify the
native resolved model/effort too. Changes to instructions, tools, providers,
permissions or any other parsed field still stop. TOML formatting/comments do not
change effective settings. Record exact initial/final hashes and changed key names;
do not publish private configuration contents.

Raw rollout capture must occur even when a configuration check fails. A retained
artifact is not economic qualification. No global configuration change, new Codex
home, credential copying or output cap is required. This wrapper is versioned;
the V1/V2 observed-session behavior and failure receipts stay unchanged.

Offline validator and retention tests must pass, followed by a native thread/start
and close with no turn/start to verify the new wrapper without inference. Then run
one fresh pair per same three contracts. No profile optimization or selective retry.
Preserve every result and cost. Same finite capability, scope and stop conditions as
V2. Known-task repeated measurement is not an unseen holdout or general parity proof.
