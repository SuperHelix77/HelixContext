# Corrected Luna varied coding artifacts

Exact source, protected tests/settings, and six model-generated implementations
from the configuration-audited three-pair continuation. The manifest binds the
files, frozen grader, published paired audit, and native stream/source identities.
Private configuration snapshots and raw environment-bearing rollouts stay local.

Run from any directory with Python's standard library:

```sh
python3 docs/research/continuation-contract/varied-coding-artifacts/verify.py
```

This verifies hashes, protected bytes, and the original 13 public tests plus
1,389 independent finite cases per arm in temporary staging. It invokes no model
and does not regenerate or mutate the published implementations. Passing these
checks is finite behavior evidence, not general capability/workflow parity.

The first candidate's native turn completed before a configuration guard stopped
the wrapper. Its exact output was recovered after auditing the native client's
automatic project-registration change. The original failure is preserved. This is
an explicit audit amendment, not an untouched preregistration or a secret holdout.

See [the report](../LUNA_VARIED_CODING_REPORT_20260910.md) for all attempts and limits.
