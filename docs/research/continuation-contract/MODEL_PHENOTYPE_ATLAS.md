# Model Phenotype Atlas: observable activity, not hidden cognition

OBSERVED:155modelsegments across34completed, raw-hash-verified streams:
Luna7streams/58segments, Sol6/13, Astra21/84. Each segment's native input/output,
cachedinput and reasoningoutput reconcile to its terminal receipt. Raw reasoning
content is neither inspected nor classified. Source and item hashes remain auditable.
No native model call was launched for this atlas.

The five requested tags are attached to visible commands, patches and messages:
semantic choice (possible judgment/probe), evidence acquisition, mechanical
verification, state transition, and serialization/reporting. Final answers can
contain both a decision and serialization. Commands may read evidence, verify
hashes and execute a novel semantic probe together. Heuristics are explicitly
fallible; task-specific human review is still required before policy decisions.

The JSON reports **nonadditive segment-association shares**, not F_m(k), the fraction
of tokens causally spent on class k. A segment's full reasoning/input cannot be
assigned to a visible command. Delta_m(k), the safe counterfactual saving from
caller ownership, remains UNKNOWN without an intervention. No overlapping shares
may be summed. Unknown hidden activity persists even when a segment has a visible tag.

## Model hypotheses and current evidence

| Model | Bounded evidence | Verdict and next attack |
|---|---|---|
| Luna |49passive ACKcontrol turns dominate input; V5one-segment final passes current gates at160output; proposal-reviewV6 rises to348 |State dispatch survives on this fixture. Not a universal continuity phenotype; capability bench pending. Stop final-format-only inference sampling.|
| Sol |IntegratedV3control8segments versus candidate1; caller registration/completion leaves semantic output |Decide/delegate hypothesis strengthened on assembly tasks. No claim it transfers to general coding. Preserve exact mechanics and minimal familiar decisions.|
| Astra |Prepared valid candidate4segments:19,560/20,140/25,374/26,469input; final2contain novel-probe/judgment work. Trust safety still discovers/reads/tests but omits regenerated hash verification |Evidence/assurance hypothesis survives, but semantic adequacy must stay model-owned. Test resident checker evidence before more authority wording.|

Astra has62mixed segments by this annotation scheme, so treating all verification
or acquisition-associated tokens as removable would exaggerate savings. Luna's
aggregate is dominated by one deliberately passive episode; it cannot be compared
to Astra's active coding/safety mix as an intrinsic model property. Sol's smaller
sample and development selection similarly prohibit probability forecasts such as
80–90%success on unseen workloads.

## Next Astra controlled test specification

Use the same task and exact files across conditions, with native tools and High
reasoning unchanged. E0proposal+contract; E1adds complete checker source; E2adds
caller-bound mechanical facts; E3adds an independently auditable coverage map with
explicit uncovered obligations. Do not claim the map proves completeness or use
the grader to choose what is salient. Existing E2-style receipts are background,
not matched controls for fresh E1/E3runs.

Before native launch, construct all four packets offline and bind byte identities,
counts, hashes and instruction release. Check whether E1actually removes source
retrieval obligations visible in the old trace. Charge extra first-turn evidence:
fewer segments can still increase cost. Require a valid task and a mechanically
passing but semantically defective counterpart; a condition that suppresses novel
semantic challenge is rejected. Unknown outcomes/stale evidence must retain
ordinary retrieval and re-entry. Freeze the test order and no-retry policy.

Report segments, completed command receipts, unmatched command hooks, distinct
new semantic probes, exact outputs and native total/uncached/output tokens. An
observed4→1/2transition is a mechanism result, not automatically80/80or capability
parity. Do not delete semantic review to meet a target. No new four-arm sweep is
launched by this atlas; it narrows and specifies the next intervention.

## Client base boundary

Sol/Luna share the current catalog base exactly; Astra's is4,110proxy tokens
versus3,552. The installed loopback serializer replaces its outgoing base fragment
with a custom file, represented as a developer item in Lite. Hosted-service
addition remains UNKNOWN. See`BASE_WIRE_PROBE.md`. Neither prompt-size comparisons
nor model descriptions explain internal cognition or constitute a native ABI test.

Reproduce:`model_phenotype_atlas.py <research-root> <atlas.json>`. Raw request and
model content remain local; public results preserve segment usage and annotations.
