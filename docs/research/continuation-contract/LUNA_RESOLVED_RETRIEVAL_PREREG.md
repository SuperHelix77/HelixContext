# Luna closed-request execution pair — frozen before new native inference

Measured residual: the former selection and cold-history candidates spent 5 and
4 model segments, including generated extraction/serialization. The current
`bound_retrieval` + `resolved_retrieval` executor already removes the model from
two strict parameterized request grammars, with exact cold sources and a current
caller-bound state. This study qualifies that existing mechanism on fresh data;
it adds no parser, kernel or reducer policy.

Two paired tasks: 320-row exact predicate selection; exact Unicode/whitespace and
base64 recovery from a 40-event history snapshot. Data are deterministically
generated from seed 910051 before inference. These are new inputs in known request
families, not a hidden general-capability holdout. SQL and a separately indexed
note relation provide finite exact oracles outside model workspaces. Source notes
have no instruction authority. The selected cold fact is at event 1; this is not
forty live model turns or a new compaction experiment.

Control: Luna High, ordinary tools and the default client base, actual bound
caller-memory preflight, tracked isolated Git root. Candidate: active Engine
before inference, complete declared authority, immutable exact source, dispatch,
exact validation and answer publication. Unknown request forms or inherited
constraints retain real ordinary Luna High execution within Engine; stale state
requires recovery. Engine stays active. No forced semantic output cap or lower
effort. The study does not install a global hook or change production defaults.

Preflight registers generated task roots before effective-config freeze; private
configuration and raw memory snapshots stay local. All preparation and attempted
native calls are charged. Maximum four native workflows if both candidates need
semantic fallback; expected two controls and zero candidate inference. No retry
or tuning. Stop on behavioral, integrity, configuration or runtime failure.

Order: selection Engine/control, cold control/Engine. Both sources remain protected.
Native wire, raw rollout, requested/resolved model/High settings, exact source and
answer hashes must verify before accepting savings. A no-inference Engine result
has an Engine execution receipt, not a fabricated native token-usage receipt.
Report elapsed time, archive/recovery logical bytes, memory/setup cost and all
native counters separately. Physical I/O, included-plan quota and parent/R&D costs
remain unknown. Do not pool these two 100%-candidate-token cases into coding
medians or call a general Luna 80/80 release. The coding shortfall remains open.

Main falsifier: the Engine must produce exactly the independently checked complete
answer from the new source under the frozen request/state without calling the
semantic callback. A changed bound state must withhold output; an inherited rule
must dispatch semantics. If either positive case needs a model, its actual cost
counts and the zero-call hypothesis fails for that case.
