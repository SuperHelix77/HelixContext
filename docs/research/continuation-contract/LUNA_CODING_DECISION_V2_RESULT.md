# Luna coding V2: workflow recovered, 71.39/65.22 measured savings

OBSERVED: One adaptive candidate under default client base instructions, Luna High, ordinary tools and unchanged workspace-write sandbox. Frozen runner `c46ac4b`. Earlier native control reused; no fresh-pair or general capability claim.

| Metric | Retained native control | Candidate | Saved |
|---|---:|---:|---:|
| Input | 134,764 | 38,562 | 71.39% |
| Output | 3,525 | 1,226 | 65.22% |
| Uncached input | 24,940 | 10,402 | 58.29% |
| Reported reasoning output | 2,114 | 857 | 59.46% |

The candidate used one submitted semantic turn and two model segments. It returned only Python source. Engine staged that implementation, ran the unchanged public tests plus 4,433 independent integer-allocation cases, verified protected inputs, and published the checked source. No model reentry was required for pending checks or publication. The original broken implementation was rejected by the same caller path in an offline preflight test.

The code, native totals, individual segment charges, exact raw evidence and protected-file hashes were audited. This finite fixture passes; intelligence/workflow parity beyond it remains unproven. The previous caller attempt's 5,361-output regression and workflow stop remain retained.

## Exact remaining model work

The raw rollout contains one tool call: Luna repeated `codex_mem recall` for the allocation task. It failed while trying to open the global longflow `server.log` under the benchmark sandbox. This is the first exact tool evidence for this candidate; do not retrofit its command identity onto older ephemeral runs.

The prompt said memory consultation was caller-completed. The global AGENTS instruction nevertheless unconditionally required recall for every substantive task. OBSERVED: Luna executed the duplicate recall. INFERRED: explicit instruction-level recognition of caller preflight is a plausible missing integration contract. The trace does not prove which instruction caused the choice, nor does it expose hidden reasoning.

First segment: 18,969 input / 451 output, including 341 reported reasoning tokens. Second: 19,593 input / 775 output, including 516 reasoning tokens. Removing a call is not equivalent to subtracting all its tokens: necessary reasoning may move. Even the conditional subtraction of the first segment leaves 775 output, above the 705-token 80%-saving budget. Therefore simply deleting the recall is not proof that 80/80 will follow.

## Costs and limits

Durable raw capture copied 84,523 bytes once for reading and once for writing, plus index metadata. Caller check/publication latency is in the JSON. Full SQLite/storage/physical I/O and research costs remain incompletely instrumented. Candidate persistence differs from the ephemeral control. No monetary or included-plan quota claim is made.

N=1 adaptive coding comparison; its median is the same observation and must not be pooled with W50 continuation or the three semantic-safety tasks. Model effort and tools were preserved; there was no output-token cap.

## Next change

Define a bound caller-memory-preflight contract that can satisfy the applicable memory instruction once, including query, task scope, completion/availability and evidence identity. Optional memory failure must continue normally, not trigger a duplicate startup inside the model sandbox. Do not broaden sandbox access, replace failure with a false success, or exempt unrelated/new tasks. Evaluate the instruction change separately before another native run.
