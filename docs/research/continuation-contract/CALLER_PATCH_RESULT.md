# Astra caller-owned patch realization: input target crossed, output below target

**OBSERVED — fresh development coding pair, not representative parity.** Both
Astra High arms repaired the same broken module under the same complete alias-safe
contract and passed 2,745 finite sequences / 8,282 transitions. Neither received
the reference repair. The candidate returned its own full source; caller copied
those exact bytes and ran the frozen checker. No retries or budget amendments.

| Native measure | Control | Caller realization | Saved |
|---|---:|---:|---:|
| Input | 101,159 | 19,940 | **80.29%** |
| Output | 1,135 | 387 | **65.90%** |
| Cached input, subset | 80,640 | 11,264 | — |
| Uncached input | 20,519 | 8,676 | **57.72%** |
| Reported reasoning, output subset | 48 | 124 | — |
| Model segments | 5 | 1 | — |
| Model commands | 5 | 0 | — |
| Native elapsed seconds | 53.37 | 15.87 | — |

This result does **not** establish 80/80, monetary savings of 80%, universal
intelligence preservation, long-horizon parity or the representative three-model
suite. Admission must not promote it as an 80/80 qualified cell.

## Verification and causal scope

The prospective manifest was published at `7a89817` before inference; its hash is
`d6a8558f8b1042446343a62d13b60dd7454d2943856cc2ede12cfb68831391d6`.
The randomized order was native then Helix. Final native cumulative usage was
independently compared with raw wire receipts; no cumulative-update double count.
Raw and normalized trace hashes matched status. Contract/checker bytes were
unchanged. Candidate artifact bytes exactly match its returned JSON source.

The two repairs have identical Python ASTs when location attributes are omitted;
formatting differs. This supports equal implementation behavior for the stated
contract, not equality of all observations such as source inspection or traceback
line numbers, and not general agent capability. Both model assessments discharged
current semantic obligations. Candidate accurately said final execution was still
pending; only the caller's later passing checker completed the workflow.

The candidate was allowed ordinary tools and exact retrieval, but issued no
commands. It generated the actual repair rather than selecting a supplied answer.
Caller-owned realization thus transferred to a bounded coding task, unlike the
previous narrow receipt-review attempt. One pair does not isolate every prompt,
cache or order effect, or prove persistence under adverse recovery.

## Costs retained

Native elapsed excludes the caller's post-return checker. That check took roughly
0.214 seconds for the candidate and 0.210 seconds for control; candidate initial
failing preflight is separately recorded in the manifest/result. Skill registration
is inside native wall time. Exact materialization CPU/I/O, fixture preparation,
SQLite/physical I/O, coordinator research and complete billing are not fully metered.
Do not replace these unknowns with zero. The frozen native counters include all
reported output, including reasoning. No compression claim uses token proxies.

## Residual output diagnosis before another call

At the observed control, 80% output saving permits **227** candidate tokens. The
candidate used 387, including 124 reported reasoning tokens. Its source is 86
`o200k_base` proxy tokens; its assessment is 135; control's final assessment is 81.
The proxies are not an additive partition of native output and must not be
subtracted as measured savings.

**INFERRED:** repeated assessment/reporting is now a more plausible output target
than another terminal reducer; there was no candidate terminal output. The emitted
repair itself is necessary task output, and its semantics were chosen by Astra.
Do not shorten code into unfamiliar encodings, lower reasoning, suppress unresolved
obligations, or count the caller's final report as free.

**HYPOTHESIS:** a common explicit semantic-disposition contract could let Astra
return its repair plus unresolved obligations, while caller renders verified test
status and the deterministic diff. Any reduction of prose must preserve the user's
required explanation and the agent's ability to refuse completion. This needs a
new prospective pair with equivalent delivered results; the old control cannot
serve as its denominator. The current candidate is frozen as bounded development
evidence. No second optimization or production route was activated in this run.
