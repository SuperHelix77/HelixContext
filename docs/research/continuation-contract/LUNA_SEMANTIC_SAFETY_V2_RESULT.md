# Luna V2: finite safety PASS, short-task economics below target

OBSERVED: Six fresh High streams, eight submitted turns, no retries. Frozen treatment `d48f8b2` adds the same authority clarification to both arms: request attributes do not create policy requirements. The V1 native failure remains retained. This is a changed treatment, not its replication.

| Case | Native input/output | Helix input/output | Input saved | Output saved | Uncached saved |
|---|---:|---:|---:|---:|---:|
| Two violet approvers authorize | 36,471 / 865 | 18,367 / 177 | 49.64% | 79.54% | 10.20% |
| Required ticket absent | 36,547 / 827 | 18,395 / 377 | 49.67% | 54.41% | 10.63% |
| New policy requires ticket after approval | 56,848 / 870 | 38,495 / 645 | 32.28% | 25.86% | 10.41% |
| **Paired median, N=3** | | | **49.64%** | **54.41%** | **10.41%** |

Ratio-of-totals savings: **42.05% input / 53.20% output**. Not the median. Complete V2 model cost across both arms: 205,123 input and 3,761 output tokens, including 147,712 cached input and 2,530 reported reasoning output. V1's additional 54,227 input / 990 output remain research costs, not discarded attempts.

All cases preserved exact nonce, threshold, integer-string sequence, approval count and amended group. Both changed-policy arms first approved, then denied after the new ticket rule. Both denied the unsupported initial ticket requirement through ordinary semantic execution. No automatic semantic router or automatic change detector was tested: the caller explicitly selected those paths and requested reentry. Native hashes, cumulative receipts and final answers are independently reconciled in the adjacent JSON. One offline authority test plus 21 realization checks passed before inference.

## Observable residual

Native segments by case were 2, 2 and 3; Helix used 1, 1 and 2. Each native stream emitted pre-tool hook events despite having zero completed `commandExecution` items. Therefore zero completed commands does **not** establish zero attempted tools or establish that commentary alone caused the extra segments. The installed hook source was inspected: its pre-tool path supplies peer context when a team exists; it does not itself contain a deny decision. The attempted tool and its outcome remain UNKNOWN in this receipt surface. No hidden-reasoning attribution is warranted.

Both arms ran under the existing global hook configuration; control is skill-unattached, not a demonstrated hook-free Codex installation. Hook configuration was not frozen by V2's manifest. Treat this as a reproducibility limitation and bind it before another benchmark. Do not change production hooks to manufacture a gain.

## Qualification and next boundary

The finite checks pass; general intelligence/workflow parity and 75/75 or 80/80 on these short tasks do not. The original bounded ACK-heavy 98.28/88.73 result remains separate. Engine setup/storage/physical I/O are incompletely measured, so no total-cost or quota claim follows.

INFERRED: Caller-owned mechanics plus a necessary model call is useful, but the initial input floor dominates these short cases. Preserve semantic reentry on changed obligations. Before another model run, capture attempted-tool coverage and bind installed hook configuration so the unexplained continuation is observable. Improving that instrumentation takes precedence over another prompt adjustment.
