# Sol High coding transfer V1 — 2026-09-10

**OBSERVED: finite behavior passes, 75/75 coding qualification fails.** Three fresh
pairs give median **88.02% input / 62.91% output / 21.50% uncached-input savings**.
All six artifacts pass the same 13 public and 1,389 independent cases per arm.
One pair meets 75/75; none meets 80/80. General intelligence/workflow parity remains
unproven. Sol V3's assembly result is a separate bounded candidate.

| Task | Control input / output | Helix input / output | Input saved | Output saved | Uncached saved | Finite checks |
|---|---:|---:|---:|---:|---:|---|
| Intervals | 139,080 / 2,227 | 16,665 / 826 | 88.02% | 62.91% | 35.91% | PASS |
| Dependencies | 120,796 / 2,073 | 16,640 / 512 | 86.22% | 75.30% | 21.50% | PASS |
| Transactions | 144,163 / 3,110 | 16,811 / 1,421 | 88.34% | 54.31% | 3.62% | PASS |

The ratio of totals is **87.60% input / 62.77% output** saved. This is not the
median; neither statistic makes the two failed output cells economically
qualified. No Engine-only retrieval or W50 result is pooled into this cohort.

## Frozen comparison

[Preregistration](SOL_VARIED_CODING_V1_PREREG.md), runner and base were committed
as `10ca97d` before inference. Manifest SHA-256:
`1d612c1450a23523b947c65fa2b1ea228a3b18c0046fa5b982a31f33dbe276a5`.

Same native `gpt-5.6-sol`, High effort, ordinary tools, task contracts, public
tests and actual scoped memory preflight. Each arm had its own tracked Git root.
Project registration completed before config freeze, with no turn/start requests.
Only already overridden GUI model/effort defaults could change; effective native
settings and all other parsed config values were verified. No guard amendment,
reused control, retry or policy edit occurred during this suite.

Control used the default client base. Candidate used the existing delegation
kernel (LUNA POLICY heading renamed DELEGATION POLICY), native Helix skill
attachment, exact files resident and caller-owned staging/checks/publication.
This is a composite transfer, not an isolated causal estimate for one mechanism.
No solution or task-specific algorithm hint was supplied. These task contracts
were previously exposed in Luna research; this is not a population holdout.

Frozen randomization happened to put the candidate first in all three pairs.
Cache state was observed, not controlled or balanced. The headline input reduction
must not be represented as monetary or uncached-input savings. The transaction
candidate had zero reported cached input: **16,811 uncached** versus **17,443**
for control. Its 88.34% total-input headline therefore means only 3.62% uncached
input saved on that pair.

## Observable execution and residual

| Task | Control segments / tools | Helix segments / tools | Control reasoning | Helix reasoning | Control / Helix elapsed |
|---|---:|---:|---:|---:|---:|
| Intervals | 7 / 6 | 1 / 0 | 869 | 578 | 88.25 / 30.74 s |
| Dependencies | 6 / 5 | 1 / 0 | 345 | 170 | 79.55 / 19.18 s |
| Transactions | 7 / 6 | 1 / 0 | 714 | 1,034 | 116.25 / 48.52 s |

Reasoning counters are a subset of output. No hidden reasoning contents or model
psychology are inferred. The candidate emits only replacement source and issues
no commands. Caller exact staging, protected-input checks and finite contract
tests succeeded, and no further semantic turn was needed.

**CONDITIONAL bound:** Holding the observed reasoning count fixed and deleting
every other candidate output token would cap interval savings at **74.05%** and
transaction savings at **66.75%**. Both are below 75%. This is not an irreducible
reasoning lower bound. It rules out solving these particular traces by merely
shortening visible source/reporting. The dependency case's corresponding bound
is 91.80%; any new mechanism still needs real measurement and capability checks.

**INFERRED:** Caller delegation removes repeated commands and much repeated input
on coding too. Its assembly output win does not transfer wholesale to novel
function implementation. Luna and Sol now show similar one-segment output
shortfalls on these contracts. More reducers, memory indexing or cold plans have
no demonstrated material token term left to remove in these completed candidate
paths. An unchanged retry would measure variability, not a new mechanism.

The Sol transaction output/reasoning counts happen to match Luna's previous
counts. Native thread IDs, raw/native hashes, requested/acknowledged model IDs,
turn-context model/effort, and generated-source hashes are distinct. No reroute
event was observed. Counts alone do not imply reused execution, identical
cognition, statistical independence or provider-internal model identity.

## Costs, artifacts and limits

All six workflows cost **454,155 input / 10,169 output**: 372,992 cached and
81,163 uncached input; 3,710 reported reasoning tokens already included in output.
There were no failed native launches or retries. Broader project/R&D failures
remain separately charged; this is not a whole-project cost total.

Preparation took **2.867 s** including six actual memory preflights, isolated Git
setup, registration and binding. Candidate memory calls took 0.141 / 0.131 /
0.138 s and retained 37,842 / 37,851 / 37,852 raw bytes. Caller checks and publication
took 0.266 / 0.278 / 0.273 s. These are partial logical/elapsed measures, not full
physical I/O, storage, CPU-energy, quota or net-cost accounting. Model elapsed
excludes some setup and closeout. No unmeasured overhead is assigned zero cost.

[Audited counters and hashes](SOL_VARIED_CODING_V1_RESULT.json) bind the raw
native receipts, exact source and native configuration. The public
[artifact verifier](sol-coding-transfer-artifacts/README.md) reexecutes all six
implementations without inference and preserves protected files. Raw rollouts and
private config/memory responses remain local. Published counters are extracted
receipts, not independent provider attestations.

The research caller checks exact initial state before staging/publication, but
has no OS-enforced isolation from an uncooperative concurrent writer. Its
two-turn experiment envelope is not a product limit. Automatic Codex-app delivery,
general capability parity and the separate 80/80 target remain open. Engine stays
active on candidate paths; a failed economic gate is not permission to disable it.

The HUD exposes the three audited pairs and their own cohort, separate from Sol
assembly and Luna. Current tariff scenarios refresh independently and are not
included-plan billing measurements.
