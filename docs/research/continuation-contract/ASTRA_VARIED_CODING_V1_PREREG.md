# Astra High coding transfer V1

Freeze before inference. Evaluate the common source-only caller boundary on
three exact existing development contracts: intervals, dependencies, transactions.
These tasks were exposed during Luna research. All six Astra runs are fresh, with
no reused native denominator, supplied solution, task-specific algorithm hint or
model downgrade. The researcher knows the finite checks; this is not a population
holdout or proof of general intelligence parity.

Control: native Astra High, default client base, ordinary tools, actual task-bound
memory preflight. Candidate: same model/effort/tools/memory workflow, frozen
delegation kernel and native-attached Helix skill, exact task files resident,
caller-owned staging/checks/publication. The kernel matches the frozen Luna base
except that the LUNA POLICY heading becomes DELEGATION POLICY. No algorithm,
semantic-review depth or tool policy is changed. No cold-plan machinery.

Reuse the already audited runner, independent task oracles, exact source parser
and realization checks. Separate tracked Git roots, native project registration
before config freeze, and effective model/effort binding address prior known
infrastructure failures. Registration must contain zero turn/start requests.
Protect tests/settings and exact inputs; retain raw receipts privately.

Six workflows in frozen per-task randomized arm order. At most two top-level
candidate semantic turns only if checks supply new failure evidence; no blind
retry/profile edit. This is an experiment envelope, not a product limit. Model
tools and semantic questions remain available. Unknown semantic state must not
be treated as a mechanically resolved request. Engine stays active in candidates.

Stop on infrastructure/config/raw-evidence failure; retain completed/failed costs.
On a behavioral failure, record it and finish only the prespecified independent
pairs. No rerun to select a favorable output sample. Audit every native cumulative
counter against per-segment updates, exact output/publication and finite checks.

Report each pair, separate input/output/uncached savings, median, ratio of totals,
segments, commands, reasoning (included in output), elapsed time, setup/memory /
caller checks, all attempted usage and missing total-cost terms. All three must
pass the finite behavior gates before economic admission. Count how many pairs
meet 75/75 and 80/80; a median does not qualify a failing cell. Do not suppress
regressions or use Engine-only retrieval to improve these coding statistics.

No general release or normal Codex-app deployment follows from a passing suite.
This result neither overwrites Astra V3 assembly nor resolves Luna's coding output
shortfall. Existing finite-check limitations, caller sandbox/atomicity limits,
parent/R&D and physical-cost gaps remain explicit.

Astra default client base was checked offline from the current native model catalog: 21,269 bytes, SHA-256 152dfaeeb552876190962be1c12c93d426840ff12691f648261554a7675a6698. Raw native control metadata must match. Unlike the old Astra V1 review kernel, this common delegation base does not add an affirmative instruction to investigate extra semantic gaps solely because Helix is present. Independent review, novel probes and necessary re-entry remain available. This composite transfer cannot causally isolate a particular clause.
