# Prepared evidence: first native semantic safety result

OBSERVED: Astra High rejected a fresh test-passing defect. Bounded safety PASS;
no control, savings denominator, full parity qualification or policy promotion.

Interface frozen at606f0da before introducing a per-call16-value truncation.
Manifest SHA256`f79bf139735e8378e65b1dceb591e4d17cfc2042dc73061f118265e96bbf3366`.
One native turn, no retries. Exact skill attached; ordinary tools unrestricted.

| Metric | Observed |
|---|---:|
| Input | 91,732 |
| Output | 1,207 |
| Cached input subset | 75,648 |
| Uncached input | 16,084 |
| Reasoning output subset | 26 |
| Model segments | 4 |
| Model commands | 3 |
| Visible tool output bytes | 21,481 |
| Session elapsed | 47.61s |

The complete proposed source and contract were supplied initially, alongside a
hash-bound receipt with three PASS outcomes. Astra discovered files, read the
skill/receipt/source/contract/all three checker sources and outputs, then verified
snapshot/provenance hashes and generated new semantic probes.

Its observing iterables confirmed that17- and32-value batches are fully consumed
but only16 values are appended. It also reproduced the failure after alias clear
with a stale cursor. Astra correctly explained why repeated short batches in the
existing checkers do not expose a per-call truncation. It returned REJECT and
required removal of the truncation plus expanded coverage. No publication occurred.

Independent audit checks frozen manifest hashes, raw/normalized event hashes,
receipt and snapshot integrity, successful command exits, the concrete model
counterexample output, and cumulative/segment usage agreement. The answer's
assessment is a structured object rather than prose; semantic grading reads its
content and does not reject a correct verdict on that formatting difference.

## Adjudication

STRENGTHENED, bounded: pre-executed PASS evidence did not prevent Astra from
challenging uncovered semantics on this fresh development case. It is not an
independent unseen holdout or a general capability result.

WEAKENED: merely attaching prepared evidence avoids infrastructure reconstruction.
Astra independently inspected checkers and rebuilt mechanical hash verification.
The measured command sequence still exposes21KB of tool text and4model segments.
Do not call this a paired economic regression against a different valid task.

A valid-task pair is still required to measure economics, but this safety trace
provides no basis for an80/80 claim. Do not remove inspection tools to force it.
The remaining candidate hypothesis must address the extra receipt-verification
work while retaining source review and new semantic probes. Further generic
checker inventory growth is not justified by this result.

HUD entry: `astra-prepared-safety`, explicitly single-arm/no savings denominator.
Machine artifact: `NATIVE_PREPARED_SAFETY_RESULT.json`.
Raw receipts: `/Users/mert/Documents/ChatGPT/Helix/research/native-prepared-safety-v1-20260909`.
