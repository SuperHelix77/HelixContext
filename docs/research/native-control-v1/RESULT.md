# Control-boundary offline closure

OBSERVED: nine scripted loopback responses through the installed app-server
completed three equivalent shell/callback/final-message paths. Shared global
continuity instructions can be omitted while ordinary memory instructions remain;
the real Helix skill can be attached separately. Usage was fabricated by the mock:
**zero hosted inference; no token-economics or capability claim**.

The first attempt correctly failed its config guard after the isolated app-server
registered an unexpected parent project. Only the private synthetic config changed.
The original failure is retained in INITIAL_FAILURE.json. Declaring the synthetic
Git root before startup corrected the fixture; production config/AGENTS stayed
unchanged in both attempts. APPSERVER_OFFLINE.json describes the corrected result.

The pure client_context projector separately preserves every non-hook setting and
removes only explicitly SHA-bound Helix hook commands. Applied to a private copy of
the actual config, it preserves the ordinary Interrupt hook, all 15 plugin entries,
both MCP server entries, features and project settings. Nine focused tests passed.
Existing trust values are copied unchanged, not fabricated. Native hook trust,
authentication and full catalog equivalence for that isolated profile remain
UNVERIFIED; the projected config was **not launched**. Do not deploy it yet.

The user has redirected priority to Astra. Its next diagnostic instead uses the
unchanged installed configuration and original base in both arms, with Helix
active in ordinary versus prepared mode. This avoids claiming the isolated profile
is qualified and labels the comparator honestly. A zero-inference actual native
preflight verifies XHigh, exact skill registration and unchanged global config;
see ../astra-reuse-v1/NATIVE_PREFLIGHT.json. It does not establish semantic parity.
