# W50 with an entirely model-written final answer

Frozen before inference, 2026-09-10. Astra is the current research priority.

The original 49 record-and-ACK requests and final semantic request remain
byte-equivalent as JSON records. The earlier authorized amendment permits Engine
to produce the exact 49 ACKs after durable persistence. The original fixture's
50-model-calls-in-both-arms instruction is superseded by that explicit user
authorization; no claim of replication under the original execution rule is made.

Unlike the previous selector experiment, **the model writes the entire final
answer**: authorization, minimum approvers, group, nonce, exact decimal and large
integer strings, evidence turns and explanatory prose. Engine does not render,
repair, normalize or replace the delivered answer. JSON is the original user's
requested final format, not an optimization-specific selector. Both arms get the
same final request and retain tools, semantic judgment and evidence retrieval.

For each separately registered effort, run one fresh Astra pair: ordinary mode
uses a persistent native session for all49 ACKs and the final answer; candidate
mode uses the same durable ledger for ACKs and one initial native semantic turn.
Additional tool segments remain available and count. Engine is active in both
arms; the ordinary path is not pristine Codex. Both receive the same installed
Helix skill and ordinary client base, with explicit High or XHigh effort. No
model/base/global-hook changes. Config and input drift stop the run.

All raw events are stored without predicting future relevance. At the final
request the caller reopens and recovers exact events from the bound ledger root;
the candidate receives every exact subject-matching record, including the hostile
vendor note. The complete archive remains retrievable. No selected policy,
amendment, answer or gold field is supplied. Semantic selection remains model work.

One fresh High pair is first. XHigh is a separate prospective cell using the same
protocol, never inherited from High. No repeated draw or prompt adjustment after
results. At most51 submitted turns per pair; additional native tool segments and
failed attempts are counted. No hidden recovery retry. Unknown/failing outcomes
remain visible and block qualification rather than generating a zero-cost success.

Offline gates: exact49-event restart recovery and unique deliveries, final/unknown
request semantic re-entry, duplicate/conflicting/stale state, failed publication,
and historical-source tamper rejection. Reuse the existing ledger's full prefix
validation; do not weaken it to claim constant-time append. Its quadratic prefix
reads and all explicit history/capture I/O must be charged and described.

Native gates:49 exact ACKs each, complete model-written final schema, exact string
values, correct authority/amendment/vendor reasoning, final byte identity,
unchanged history, recovered-event equality, original client base and explicit
effort, every usage increment and raw stream hash. Finite known-task evidence does
not establish universal intelligence, broad workflow parity or automatic desktop
pre-inference integration. This supplies only the W50 cell of the seven-cell cohort.

Report input/output/cached/uncached, reported reasoning, segments, model turns,
memory preparation, Engine time/logical I/O/history writes, recovery and setup.
Use dated/current API-equivalent scenarios separately from actual quota or total
cost. Do not use the previous selector control or headline as this pair's denominator.
80/80 remains the research target;65/65 is only Astra's first-release median gate,
not a reason to hide a below-target W50 result or change its output contract.
