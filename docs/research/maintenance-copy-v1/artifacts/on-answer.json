{"edits":[{"old":"    def search(self,project,query,limit=10):","new":"    def search(self,project,query,limit=10,match_mode='all'):"},{"old":"            raise ValueError('Invalid search request')\n        words=re.findall(r'\\w+',query,flags=re.UNICODE)","new":"            raise ValueError('Invalid search request')\n        if not isinstance(match_mode,str) or match_mode not in ('all','any'):\n            raise ValueError('Invalid search mode')\n        words=re.findall(r'\\w+',query,flags=re.UNICODE)"},{"old":"        match=' AND '.join('\"'+word+'\"' for word in words)","new":"        match=(' AND ' if match_mode=='all' else ' OR ').join('\"'+word+'\"' for word in words)"}]}