# Exact existing-code reuse: one prospective Astra maintenance pair

Status: OFFLINE PREPARATION. No native pair has been launched. Freeze the actual
runner, prompts, task inputs, graders and configuration before any model call.

The prior three-task coding suites remain failed economic qualification. This
new maintenance family cannot replace them. It tests the direction prescribed by
their anatomy: reuse correct code that existed before a changed requirement.

Baseline files come byte-for-byte from existing Helix Memory at the commit in
`BASELINE_MANIFEST.json`. Both arms receive the same files, new requirement and
public tests. The investigator knows the requirement; this is development research,
not a population holdout. Native control has ordinary editing/test tools and the
same opportunity to reuse existing code efficiently.

Candidate emits ordinary exact old/new text edits in JSON. The small literal-edit
adapter compiles them into the existing immutable source-copy plan. It supplies
no solution, codebook, schema-specific validator or new semantic instruction set.
All matches refer to the original source; absent, duplicate or overlapping spans
are rejected. Replacing the entire original file is still expressible. Candidate
receives complete relevant source and retains ordinary tools and semantic review.
Assembly, source binding, tests and publication belong to the caller; failures
require explicit repair or an incomplete result, not automatic semantic correction.

Offline selection of this representation preceded inference: a whole-method copy
recipe was 1,146 bytes, exact edits 518 bytes, and a standard diff 1,042 bytes on
the investigator reference. Native control may generate a smaller patch. No
native savings or causal reasoning reduction follows from those byte counts.

First model: Astra High, same effort/tools as prior cells. Control default base;
candidate existing frozen Astra delegation base and native skill attachment.
This is a composite product test, not a pure effect-size attribution to copy handles.
Actual scoped memory consultation and native registration happen before inference.
Both roots are isolated tracked Git workspaces. No global model/config changes.

Before native calls, qualify the grader with baseline, correct reference and wrong
implementations; exercise source drift, protected-file drift, invalid references,
unsupported output, and exact copying. Investigator reference stays outside model
workspaces. Show actual byte/proxy opportunity without converting it to predicted
native savings. Do not add solved benchmark behavior to the caller.

One fresh randomized pair; no reused control or outcome-dependent retry. Allow at
most one semantic repair turn per arm after an actual failure, and include its
cost. Infrastructure/binding failure stops globally. No native call is warranted
until the offline gate passes and the concrete runner manifest is committed.

Report input, output, cached/uncached, reported reasoning, turns/segments, tool
calls, exact artifact checks, model-visible setup, selection/assembly costs and
all attempts. Full response cost matters, including any interface rediscovery.
No prediction from retained bytes alone; native may already edit efficiently.

≥80/80 with checks permits only a bounded maintenance candidate; ≥75/75 permits
only that narrower candidate. Otherwise record the residual and stop this policy
without retuning the fixture. Existing failed coding medians remain visible.
