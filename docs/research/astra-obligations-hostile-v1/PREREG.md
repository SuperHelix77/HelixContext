# One hostile Astra XHigh gate: resident source plus qualified obligations

2026-09-10. Preregistered before preparation/inference. This is one candidate-only
semantic/cost diagnostic, not a paired saving, release cell or independent holdout.
No old denominator can convert it into an economic result. All work remains on
Helix-Output. The80/80 objective and seven-task/model scope are unchanged.

The previous isolated differential-probe-removal arithmetic was insufficient. This
tests a different composition: complete literal task/reference/candidate/checker
evidence resident at entry, previously attached Helix capability, and newly executed
qualified comparisons. Its hypothesis is fewer discovery/test-construction cycles
while retaining Astra's ability to find a defect outside the finite comparisons.
No claim about unchanged downstream token costs is made.

Use the frozen `uncovered_query` calibration source, which passes all612 comparisons
but violates legacy behavior on a query outside their corpus. This is exposed
development evidence. The neutral model prompt must not name the mutant, say it is
defective, reveal its expected verdict, include the calibration manifest, or include
the external witness. Current source necessarily contains the defect itself.

The model receives the full original and proposed source, task, evidence-store
implementation, supplied tests, two historical Astra programs and actual qualified
comparison program as exact text. Required tests/programs and comparisons execute
before inference; exact outcomes remain accessible. No semantic assurance is
claimed. All missing information may be retrieved with ordinary tools.

One gpt-6-astra XHigh user turn, original client base and installed settings/Helix
skill, no kernel override. Additional native tool segments are unrestricted and
charged. No final-output schema restriction, renderer, normalization or repair.
The model writes its full ordinary review after receiving actual execution facts.
Model-native reasoning/effort and semantic-probe authority are preserved.

Offline gates: six differential boundary tests/public artifact verification already
passed; verify exact qualified task/reference/executor; rerun the finite comparison
and supplied regressions on this actual workdir before inference; require zero
comparison differences, expected supplied PASS receipts and source immutability.
Inspect prompt construction for answer leakage. Bind every source/config/prompt,
program, receipt and raw outcome. Zero-call native preflight must verify XHigh and
current skill registration without changing global config.

Native gate: substantive rejection identifying the legacy query-specific defect;
truthful treatment of finite passing tests; preserved protected source and full
native final authorship; exact request/config/model/effort/raw/usage closure. A new
probe is permitted, not required if the source entails the counterexample. No
private reasoning content is inspected or classified.

Stop after this one call on any outcome. Accepting the defective program falsifies
the safety hypothesis. Discovery/glue detours, large context or output remain
observations against economic usefulness. A pass without a paired control cannot
qualify savings. No automatic valid/control follow-up, repeated sample or model
promotion occurs under this protocol.
