# Astra XHigh reasoning notation V1: reject the economic policy

2026-09-10. Preregistered commit `0afc80c`; one fresh synthetic two-proposal review pair. Both models used the original client base, XHigh, ordinary tools, the same active Helix Engine/skill, and complete model-written finals. Only the candidate received the 38-word / 50-proxy-token internal-notation instruction. This is not a release cohort or universal parity evidence.

| Native measure | Ordinary Engine control | Notation candidate | Saving |
|---|---:|---:|---:|
| Input | 70,785 | 122,167 | -72.59% |
| Cached input | 56,320 | 100,864 | -79.09% |
| Uncached input | 14,465 | 21,303 | -47.27% |
| Output, including reported reasoning | 3,374 | 4,477 | -32.69% |
| Reported reasoning | 801 | 1,360 | -69.79% |
| Other generated output | 2,573 | 3,117 | -21.14% |
| Model segments | 3 | 5 | — |
| Shell commands | 3 | 4 | — |

**Finite semantic/workflow checks passed in both arms. Economic gate failed. No self-application.** Each review correctly rejects proposal K for expiry-at-deadline and lost duplicate positions, accepts proposal R on bounded evidence, preserves normal prose and source files, and accurately reports the supplied eight passing tests. Caller does not write or repair either final. Review scope and normal tool access survive.

The control's independently replayed program reports R 9/9 and K 6/9 check groups. The candidate's exact pytest program reports R 11/11 and K 8/11 cases, with three expected assertion failures covering the two defects. Those failures are semantic evidence, not an infrastructure error. These counts are different test organizations, not a proof that one model is more intelligent. Both retain semantic investigation.

## Observable failure anatomy

Both reread supplied source and reused the supplied execution receipt without rerunning the public tests. Candidate additionally listed files and reread the attached Helix skill (1,695 returned bytes). It created a pytest probe file and invoked it in a later segment; control executed its probe inline. Candidate's other output grew as well as reported reasoning: the 1,103-token output increase comprises 559 additional reported reasoning tokens and 544 other tokens. No hidden reasoning content is inferred or published.

The observed behavior differs despite a small instruction change. One stochastic pair cannot identify which clause caused it, establish a model-wide effect, or falsify all languages/notations. It does falsify admission of this exact policy on this pilot. Do not hide its result, repeat it unchanged until it wins, or describe shorter notation examples as demonstrated thinking savings.

## Evidence and complete accounting limits

All native counters were reconciled to cumulative totals and per-segment increments. Client base, model, XHigh effort, native skills, effective configuration, task sources, final authorship and raw stream hashes were checked. Native prompt texts match after removing STYLE.txt and necessary task-directory paths. The offline debug gate additionally normalizes only fresh message IDs/render timestamps; its first metadata-only comparison failure is retained as an extra preparation attempt, with zero inference.

Source/usage/commands/finals/probes are preserved in artifacts. Both generated probe programs were independently replayed against immutable copies of the same source. See [machine report](RESULT.json), [semantic review](SEMANTIC_REVIEW.json), [offline gate](GATE.json), and [frozen contract](PREREG.md).

The dated official Standard API-equivalent estimate is $0.369670 control versus $0.537744 candidate (45.47% more) under the short-context tariff; long-context is 47.11% more. Rates were checked at 2026-09-10T13:39:09Z from [official pricing](https://developers.openai.com/api/docs/pricing.md); exact source hash and tariff values are in RESULT.json, original source is retained privately. The HUD refreshes rates independently. These are tariff scenarios, not measured Codex quota or complete ROI.

The pair consumed 192,952 input and 7,851 output tokens, including 2,161 reported reasoning. Successful preparation took 2.97 seconds; the earlier preparation attempt, shared public preflight, memory capture, research, grading, raw archive and storage also cost resources. Timers and archive bytes are in receipts; they are not physical-I/O or whole-project-cost measurements. No failed native attempt is omitted; there were exactly two native model turns and eight model segments across the pair.

The broader 80/80 goal remains open. This Astra result stays in the Astra lane; Luna requires its own prospective test and cannot inherit either this failure or older restricted-output wins as a parity verdict.
