# Astra XHigh reasoning-notation V1 — frozen development diagnostic

2026-09-10, before any native outcome. Research basis: thinking-representation
5408adb; existing prepared review had 558/1089 reported reasoning/output tokens.
This makes a reasoning-style pilot plausible; it does not predict 80/80.

One fresh paired two-proposal cache review, native engine path versus the same
path with only STYLE.txt added. Both have original model base, XHigh effort,
ordinary tools, the same Helix Context skill, caller registration/memory preflight,
exact source, and the same caller-executed public regression evidence. Engine
remains active. No shorter final, output schema, turn cap, lower effort, custom
base, foreign-language instruction, or suppressed tools. No user-facing final
rewriting. This is a new synthetic development diagnostic, not a fresh release
holdout or pristine native Codex. Historical maintenance cases supplied only the
admission-cost lead; no benchmark answers are attached.

Task requires complete ordinary prose reviews of both proposals. Public checks
pass both. Independent witnesses establish that proposal_k breaks expiry equality
and repeated batch positions; proposal_r preserves these and the original API.
The model must identify both defects accurately, avoid a false extension-defect
claim on proposal_r, and preserve requested checks and normal final authorship.
Inherited baseline limitations may be noted and are not automatically false
positives. No predetermined number of probes or mandatory tool calls: exact
source inspection can establish these defects without rerunning arithmetic.

Offline gates: public tests 8/8; hidden witness outcomes exact for both versions;
all task/caller/config/base/skill/CLI bindings pinned; common exact source views
and identical prompt payload except STYLE.txt and necessary workdir paths;
CLI prompt-input comparison (fresh message IDs and render timestamps are transport metadata, explicitly normalized); native thread/skill/effort setup without turn/start.
The debug-rendering check does not prove server-owned context equivalence; native
requests and effective configuration will be audited after execution.

Arm order is chosen from the low bit of TASK.md's SHA256 before the runs, then
frozen in the manifest. One run per arm, no automatic inference retries or answer
repairs. Capture any failed run before stopping. No third model call in this
pilot. Graders and expected outcomes stay outside both task directories.

Success for mechanism screening requires both semantic reviews correct, ordinary
final prose, unchanged source/config/effort, lower candidate native reasoning and
total output, at least 20% output reduction, no extra model segments, and no more
than 5% total-input increase. Report uncached input and dated API cost separately;
any weighted token-cost regression prevents economic admission. With N=1 these
are screen gates, not significance/parity certification, medians, or deployment.
A positive result requires a preregistered fresh multi-task cohort before self-use.
The active full objective remains 80/80 with capability/workflow parity.

All input, cached input, uncached input, output, reported reasoning, other output,
segments, tool calls, failures, latency, prep/check/memory/storage and capture costs
are retained. Shared preflight construction is listed once as research cost and
in full as a per-use deployment overhead, not magically free. Actual physical I/O
and included quota remain unknown. No inference is used for this grading report.

Offline preparation attempt 1 stopped before any native thread/model turn: debug comparison included fresh message IDs and render times. The preserved diff showed only those metadata changes after style/cwd normalization. Fix those two exact wrapper fields, retain all content, and prepare a new bound directory. Both preparation attempts remain research overhead.
