# Luna normal-final maintenance pair v1

This is a versioned derivative of `docs/research/native-output-v1/pilot.py`.
It changes only the release cell to `gpt-5.6-luna` at High effort and binds the
existing `luna-coding-v1-75` profile base. The tested bound edit tool, caller
memory preflight, task-local registration, ordinary tools, semantic review,
independent checks and model-authored normal final remain in force.

The candidate uses the already-tested `helix_apply_edits` interface; the control
uses ordinary execution. At most two native turns per arm are permitted, with a
second turn only after an actual checker failure. No caller-rendered final,
selector output, lower effort, output cap, notation variant or hidden reasoning
inspection is used. All attempts and failures remain charged. This is one
maintenance cell only and cannot qualify the seven-cell Luna release or 80/80.
