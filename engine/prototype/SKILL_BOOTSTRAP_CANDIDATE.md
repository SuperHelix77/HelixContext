# Skill-side bootstrap candidate

The native memory follow-up included an additional availability narrative about
helpers, without a corresponding recorded helper attempt. This is evidence of
extra narration, not proof that the skill caused it or that the helpers existed.
The candidate in `skills/helixcontext/SKILL.md` narrows the caller handoff rule:
use supplied engine results without inventing setup; resolve missing helpers when
the task or host actually requires them; ground availability reports in evidence.

Required host setup is preserved. Ordinary tools remain available. The skill does
not authorize skipping tests, suppressing real failures or trusting stale sources.
The ledger paragraph was condensed while retaining ownership, substantive-change,
duplicate-write and recoverability constraints.

The entrypoint grew from 297 to 317 `o200k_base` tokens. The additional 20 tokens
recur whenever the skill is loaded/replayed and must count against any avoided
helper discovery or narration. This is a candidate, not a demonstrated saving.

The skill-creator structural validator passed. Installed skill bytes still match
the preceding version; no hooks or invocation metadata were changed. Hashes and
counts are in `SKILL_BOOTSTRAP_CANDIDATE.json`. No native behavioral run occurred.

Before promotion, test actual decisions on at least these distinct conditions:

1. Caller supplied valid evidence and completed preflight; optional engine helper
   absent. Finish via available tools without invented prerequisites.
2. A real host-required bootstrap is unresolved. Preserve that obligation rather
   than treating the new instruction as permission to bypass it.
3. A required helper or evidence retrieval actually fails. Preserve diagnostics,
   recovery obligations and failure reporting; do not hide them for token savings.

Keep reasoning settings and tool access equal within comparison arms. Measure
the complete task, including this added skill payload and any fallback. A shorter
message alone cannot establish intelligence/workflow parity or the 80% target.
